{
  "name": "a11y-agent",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "turbo build",
    "docker:build": "docker build --tag a11y-agent-api .",
    "docker:run": "docker run --rm --init --ipc=host --publish 3000:3000 a11y-agent-api",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "turbo": "^2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.62.0"
  }
}
